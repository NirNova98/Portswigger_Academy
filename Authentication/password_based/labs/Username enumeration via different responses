Vulnerability:
	The website returnes wether the input username is valid/invalid which allowes and attacker to know what username exists in the system.

Exploit:
	Brutefore the usernames & passwords and check for differences in the response.
	
Example:
	1. Brutforce the username with a random password value, check when "Invalid username" is not in the response.
	2. Brutforce the password with the username we found in step one, check when "Incorrect password" is not in the response.
	
Prevention:
	Do not return any information about validity of an input besides that the credentials are incorrect.
